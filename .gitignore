# Dependencies
node_modules
.pnp
.pnp.js
pnpm-lock.yaml

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage

# Turbo
.turbo

# Vercel
.vercel
next-env.d.ts

# Build Outputs
.next/
out/
build
dist
abi/
**/tsconfig.tsbuildinfo

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# IDE
# sharing settings via .vscode.default and .windsurfrules.default; this is mostly for me so that I can keep consistent settings between different machines with maximum low effort but you can use the rules too by removing the .default extensions
# I **HIGHLY** recommend you only do this if you know what you're doing, because I hardly know what I'm doing, and you can mess your settings up pretty badly
.vscode
.cursor
.windsurfrules

# Foundry
cache/
broadcast/

# forge script script/DeployMinter.s.sol:DeployMinterScript --rpc-url $BASE_RPC_URL --broadcast
# forge script script/GrantRole.s.sol:GrantMintConfigRoleScript --rpc-url $BASE_RPC_URL --private-key $PRIVATE_KEY --broadcast
# forge script script/ApproveScript.s.sol:ApproveScript --rpc-url $BASE_RPC_URL --broadcast
