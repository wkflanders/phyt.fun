{
    "name": "phyt.fun",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "prebuild": "pnpm exec turbo clean && pnpm install",
        "build": "pnpm run prebuild && pnpm exec turbo build",
        "dev": "turbo dev",
        "lint": "turbo lint",
        "clean": "rm -rf node_modules && turbo clean",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "type-check": "turbo type-check",
        "db:up": "docker compose up -d",
        "db:down": "docker compose down",
        "db:start": "docker compose start",
        "db:stop": "docker compose stop",
        "db:clean": "docker compose down -v",
        "db:generate": "pnpm --filter @phyt/database run db:generate",
        "db:migrate": "pnpm --filter @phyt/database run db:migrate",
        "db:studio": "pnpm --filter @phyt/database run db:studio",
        "db:seed": "pnpm --filter @phyt/database run db:seed",
        "db:reset": "pnpm --filter @phyt/database run db:reset"
    },
    "devDependencies": {
        "@types/node": "^22.14.0",
        "eslint": "^9.24.0",
        "prettier": "^3.5.3",
        "turbo": "^2.5.0",
        "typescript": "5.7.3"
    },
    "packageManager": "pnpm@10.8.0",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "dotenv": "^16.4.7"
    }
}
